# Core framework and dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
selenium==4.16.0
boto3==1.34.0


# PDF processing (Story 1.2)
pdfplumber==0.10.3
PyMuPDF==1.24.0

# AWS Bedrock integration
botocore>=1.34.0

# Testing dependencies
pytest==8.0.0
pytest-cov==4.1.0

# Code quality
flake8==6.0.0

# Type hints and static analysis (optional but recommended)
mypy==1.8.0
types-boto3==1.39.16

# Lambda deployment requirements:
# The following layers are required for Lambda deployment:
# - Chrome binary layer (provides /opt/chrome/chrome)
# - ChromeDriver layer (provides /opt/chromedriver)
# Environment variables:
# - CHROME_BINARY_PATH=/opt/chrome/chrome
# - CHROMEDRIVER_PATH=/opt/chromedriver